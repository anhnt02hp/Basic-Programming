#include <bits/stdc++.h>
using namespace std;

int main() {
    //Declare p as pointer to int
    int *p;

    //Declare p as pointer to a pointer to int
    int **p;

    //Delcare p as an array of 10 pointers to int
    int *p[10];
    return 0;
}